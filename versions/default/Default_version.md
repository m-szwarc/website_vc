# Copy your default version here!

This directory is intended to store default files of your website.  
*You can always change default directory name in `vc_user.php` file with `DEFAULT_VERSION` constant.*
